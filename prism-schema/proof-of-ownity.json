{
 "name": "Link between DID and Address",
 "version": "1.0",
 "id":"https://proofspace.id/prism-schemas/unspend-output",
 "description":  "proof, that address linked to did have unspend tokens connected with address. They are not locked, verifier should check that this output is not spend",
 "properties": {
    "tag": { "type": "string" },
    "address": { "type": "string"},
    "amount": { "type": "decimal" },
    "tokenName": {  "type": "string" },
    "transactionId": { "type" : "string" },
    "issueDate": {
	 "type": "timestamp",
	 "timeOf" : "issuing"
    }
 }
}

